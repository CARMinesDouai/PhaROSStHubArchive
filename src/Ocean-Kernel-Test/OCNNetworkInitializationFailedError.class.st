"
Comment from old network library from Squeak:
Signals that no network was found. This could happen, e.g., on dial-up connection when no connection was established when Pharo tried to access it.


"
Class {
	#name : #OCNNetworkInitializationFailedError,
	#superclass : #Error,
	#category : #'Ocean-Libraries-Plugin'
}
